import 'package:flutter/material.dart';

class Esraa extends StatelessWidget {
  const Esraa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: [
        IconButton(onPressed: () {}, icon: const Icon(Icons.favorite)),
      ]),
    );
  }
}
